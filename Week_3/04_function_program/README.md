Purpose: To calculate the ss tax using gross pay and hours worked.

Code Explanation:

I have entered 4 functions. The first is used to get the Gross Pay based on the number of hours and rate of pay of the employee.  These two variables are defined on line 21 of the code. 
The second function calculates the social security tax using the gross pay that was calculed in the previous function and multiplying it by the SS rate.  This is a hard number and is defined on line 22 of the code at 6.20%
The third function is the net pay, which is done by taking the gross pay and subtracting the social security amount which are done in the previous two functions. 
The last function is to get all the pay infomation ready for display.  This take the return results from the previous three functions and put them together for the print output. 
The last section of the code is where the items are defined for the print pay information function.  THe employee name, number of hours and pay rate are all entered amounts. 

I ran this program using different variables to make sure it worked after changing the hours and the rate of pay.  It worked as expected each time. 